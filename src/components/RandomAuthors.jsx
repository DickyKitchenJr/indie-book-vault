// PURPOSE: take all authors in each genre and randomize them into a new array of objects
const randomAuthor = (Authors) =>{
    // PURPOSE: get total count of authors
    let count = Authors.length;
}

export default randomAuthor;